**SectionHeading** — the recurring section intro: optional blue eyebrow, bold title, muted lead constrained to a readable measure.

```jsx
<SectionHeading
  eyebrow="Research Tracks"
  title="Current Research Tracks"
  lead="Active and near-term work extending our foundations into distributed systems." />
```

`align` is `center` (default) or `left`.
