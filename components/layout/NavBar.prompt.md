**NavBar** — clean sticky top nav: translucent cream bar with a blur, logo lockup left, links + optional CTA right.

```jsx
<NavBar active="programs" onNavigate={go}
  items={['Programs','Method','Founder','FAQ']}
  cta={<Button size="sm">Apply</Button>} />
```
