**Input** — sunken dark text field with a blue focus ring; optional stacked `label`.

```jsx
<Input label="Full name" placeholder="Jane Doe" />
<Input type="email" label="Email" placeholder="jane@example.com" />
```

Native input attrs pass through (`type`, `required`, `value`, `onChange`, …).
