**Badge** — bold figure/label chip for citation counts, "Most popular" flags, inline metrics.

```jsx
<Badge>235</Badge>
<Badge tone="coral">Most popular</Badge>
```

Tones: `indigo` (default), `coral`, `sun`, `solid`, `success`.
