**Logo** — the brand lockup. Indigo circuit-cube mark + `Elvon Labs` wordmark (Bricolage Grotesque). The mark is always indigo; the wordmark inherits `color`.

```jsx
<Logo size={24} />
<Logo markOnly size={32} />
<Logo color="var(--text-on-ink)" />  {/* on the dark ink panel */}
```
