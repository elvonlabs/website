**Select** — dropdown matching the Input field, with a custom chevron.

```jsx
<Select label="Current role" placeholder="Select one"
  options={['PhD / Graduate student', 'Industry researcher / Engineer', 'Student (undergrad)']} />
```
